package test

import "core:fmt"

S :: struct {
	field1, field2: int,
}

main :: proc() {
	s: S = {
		field1 = 69  + 1 * m,
		field2 = 420 * a,
	}
	fmt.println("Hello World")

	when false {
		testing
		another 
	}
}
